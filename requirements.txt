# core (pinned to what the GRPO stack needs — see docs/handoff.md for the version-hell notes)
torch>=2.4
transformers>=4.52,<5
trl>=0.19,<0.20
datasets>=3.6
peft
accelerate
numpy
# serving / worker pool
litellm
# training config + reward
hydra-core>=1.3
omegaconf
math_verify
# artifact fetch
huggingface_hub
